# task-manager-api
The Task Manager API is a simple backend built with Express.js and Node.js. It supports full CRUD operations: users can create tasks, view tasks with optional status filters, update details, mark tasks completed, and delete tasks individually or in bulk. Tasks are stored in memory, making it ideal for learning REST APIs and testing with Thunder Client.
